# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_ANON_KEY=your-anon-key-here

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-key-here
OPENAI_MODEL=gpt-5-mini

# Google Cloud Vision
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_CLOUD_API_KEY=your-google-cloud-api-key
# Alternative: Use service account JSON file instead of API key
# GOOGLE_APPLICATION_CREDENTIALS=./path-to-service-account.json

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PRICE_BASIC_MONTHLY=price_1SNM0FKDELibqlGW3UNWPICO
STRIPE_PRICE_BASIC_ANNUAL=price_1SNM0GKDELibqlGWNQeAqxWq
STRIPE_PRICE_PRO_MONTHLY=price_1SNM0GKDELibqlGWixyfkhuy
STRIPE_PRICE_PRO_ANNUAL=price_1SNM0HKDELibqlGWhIMtUjKk
STRIPE_PRICE_UNLIMITED_MONTHLY=price_1SNM0HKDELibqlGWSJtge0Gv
STRIPE_PRICE_UNLIMITED_ANNUAL=price_1SNM0HKDELibqlGWX7Ay472V

# App Configuration
# Production URLs (default)
APP_URL=https://black-pill.app
API_BASE_URL=https://api.black-pill.app

# Local Development URLs (uncomment these for local dev)
# APP_URL=http://localhost:8000
# API_BASE_URL=http://localhost:7000
#
# Note: APP_URL is used for:
#   - Share card generation (referral links)
#   - Stripe checkout success/cancel URLs
#   - Subscription redirect URLs
# For local dev, set APP_URL to where Flutter web runs (usually port 8000)

# Redis (for rate limiting - Upstash)
REDIS_URL=redis://default:password@host:port

# Email (Resend)
RESEND_API_KEY=re_your_resend_key

# Firebase Admin SDK (for sending push notifications)
# Option 1: Path to service account JSON file (local development)
GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json
# Option 2: Service account JSON as single env var (for Vercel/production - copy entire JSON content)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# Security
JWT_SECRET=your-jwt-secret-here
CRON_SECRET=your-cron-secret-here-for-scheduled-jobs

# Environment
NODE_ENV=development

